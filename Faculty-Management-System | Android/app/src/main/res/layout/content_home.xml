<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_home"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.amrita.fmsamrita.Home"
    tools:showIn="@layout/app_bar_home">

    <Button
        android:id="@+id/button_timetable"
        android:layout_width="150sp"
        android:layout_height="150sp"
        android:layout_centerHorizontal="true"
        android:background="@drawable/round_button"
        android:elevation="20dp"
        android:onClick=""
        android:text="@string/time_table"
        android:textColor="#fff"
        android:textSize="23sp" />

    <Button
        android:id="@+id/button_resources"
        android:layout_width="150sp"
        android:layout_height="150sp"
        android:layout_below="@id/button_timetable"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/round_button"
        android:elevation="20dp"
        android:text="@string/resources"
        android:textColor="#fff"
        android:textSize="23sp" />

    <Button
        android:id="@+id/button_meetings"
        android:layout_width="150sp"
        android:layout_height="150sp"
        android:layout_below="@id/button_resources"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="10dp"
        android:background="@drawable/round_button"
        android:elevation="20dp"
        android:text="@string/meetings"
        android:textColor="#fff"
        android:textSize="23sp" />

</RelativeLayout>
